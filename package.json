{
  "name": "digital-business-card-ai",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@getbrevo/brevo": "^2.2.0",
    "@google/generative-ai": "^0.12.0",
    "@supabase/supabase-js": "^2.43.5",
    "cheerio": "^1.1.2",
    "googleapis": "^142.0.0",
    "ioredis": "^5.8.2",
    "redis": "^5.10.0"
  },
  "scripts": {
    "inject-ratings": "node js/inject-ratings.js",
    "generate-knowledge": "node js/generate-knowledge.js",
    "generate-sitemap": "node js/generate-sitemap.js",
    "build": "npm run generate-knowledge && npm run generate-sitemap && npm run inject-ratings && mkdir -p public && cp -r *.html *.css *.svg *.csv js css images Font CSV-Importer-PRO downloads public/ 2>/dev/null || echo 'Some assets missing, continuing...'"
  }
}
