{
  "name": "digital-business-card-ai",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@getbrevo/brevo": "^2.2.0",
    "@google/generative-ai": "^0.12.0",
    "@supabase/supabase-js": "^2.43.5",
    "googleapis": "^142.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo 'Building...' ",
    "postbuild": "node generate-sitemap.js"
  }
}
